{
  "name": "@algoguido/types",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript types, DTOs, and Zod validation schemas",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "eslint .",
    "clean": "rimraf dist .turbo"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
