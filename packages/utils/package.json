{
  "name": "@algoguido/utils",
  "version": "1.0.0",
  "private": true,
  "description": "Shared utility functions — formatting, pagination, error handling",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "eslint .",
    "clean": "rimraf dist .turbo"
  },
  "dependencies": {
    "@algoguido/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
